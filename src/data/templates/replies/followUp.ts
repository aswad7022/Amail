export function buildFollowUpReply(
  reference: string
) {
  return `Dear Customer,

Thank you for your follow-up regarding transaction ${reference}.

The review is still in progress.

Our Compliance Department has not yet completed the investigation.

If additional documents are required we will contact you.

Thank you for your patience.

MoneyGram Customer Service`;
}