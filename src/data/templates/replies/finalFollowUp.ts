export function buildFinalFollowUp(
  reference: string
) {
  return `Dear Customer,

Your transaction ${reference} is still under compliance review.

Unfortunately we cannot provide an estimated completion date.

Once the review has been completed you will immediately receive an update.

Kind regards,

MoneyGram Customer Service`;
}